#!/usr/bin/env bash

echo -e "\e[0;32mInstalling sudoless access for user ivan\e[0m"

echo "ivan ALL=(ALL) NOPASSWD:ALL" | sudo tee -a /etc/sudoers.d/ivan > /dev/null

sudo chmod 0440 /etc/sudoers.d/*